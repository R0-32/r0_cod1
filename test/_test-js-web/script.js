'use strict';
debugger;
const head = [1,2];

let rClone = [];
    
for(let i=0;i<head.length;i++)
{
	rClone[i]=head[(head.length-1)-i];
}

for(let i=0;i<head.length;i++)
{
    if (rClone[i] !== head[i]) {
        //return false;
        const result = 0;
    }
}

const result = 1;
//return true;
