
python --version
ctrl+z+enter - выход из python
// Чтобы проверить переменную среды PATH в терминале, вы можете выполнить следующую команду: 
echo $PATH
// Чтобы проверить путь к Python 3.11.3
which python
// Для проверки установленных пакетов Python 
pip list

\S+@\S+\.\S+
— и адреса
(http|https)://[a-zA-Z0-9./?=_%:-]*
— быстрое чтение

S H O R T C U T    K E Y S
${hr}
chmod +x <имя файла> // сделать исполняемым
ctrl+C // прервать
ctrl+z // приостановить
fd // продолжить
bg // в фоне // <имя> &
reboot //
poweroff или shutdown -h now //
systemctl reboot или systemctl poweroff // in systemd

cp file.txt ~/folder/ // копировать в директорию
cp -r <название> // дублировать директорию
mv <откуда> <куда> // перемещение

mkdir <название> //
mkdir -p <путь название> //
touch <название> // создание файла

rm -r dir // удаление директории со всем содержимым
rm -rf ./ // удаление вложенного

df Th // инфа о системе
id // инфа о юзере
pwd // текущая директория
dirs -l // полный путь
ls // содержимое
  -la //список в том числе скрытые файлы 
  -lat // по последним изменениям даты и времени

<комманда-прога> --help 
man — справка для перегляду документації.
cat <имя файла> | grep "name" 
head — для показу перших кількох рядків файлу;
tail -f <файл> — последние строки
foo — последние
touch — создать
clear — очистить
