#установка
	#имя флешки
		lsblk
	#размонтировать
		sudo umount /dev/sdb1
	#форматнуть
		sudo mkfs -t ext4 -L FLASH /dev/sdb1
	#создать установочную флешку
		sudo dd bs=4M if=/home/robor/Downloads/beryllium-1-i386.hybrid.iso of=/dev/sdb status=progress oflag=sync

#Ньюанс по установке	
	// инфа о вайфае
		inxi -Nxxxz	
		//  Inxi сообщит, правильно ли ваш компьютер распознал ваш Wi-Fi-чип и загрузил правильные драйверы
		
		sudo apt-get install grub
		grub-install /dev/sda
		update-grub
		https://forums.bunsenlabs.org/viewtopic.php?id=7520

	#загружаемся с флешки выбираем start install
	#смотрим внизу екрана клавишу редактирования это TAB или «E»
	#видим екран и строку в которой нужно ввести ipv6.disable=1
		перед -- quiet
	Ctrl-x - чтобы загрузится с измененной записи

bl-welcome
	старт
	isenkram-cli - ut for install

# install
	sudo apt update && sudo apt upgrade
	sudo apt install mono-complete synaptic ibus fancontrol ffmpeg flameshot neofetch msttcorefonts audacity git code
	telegram download from website
	vs code
		install binary
		2clic
		
How to change lang?
	setxkbmap ru

	#or
		sudo dpkg-reconfigure keyboard-configuration
		#переключение в ibus
			gsettings set org.freedesktop.ibus.general.hotkey triggers "['<Alt>Shift_L']"
		sudo service keyboard-setup restart

	/etc/default/keyboard
		# KEYBOARD CONFIGURATION FILE
		# Consult the keyboard(5) and xkeyboard-config(7) manual page.
		XKBMODEL="pc105"
		XKBLAYOUT="us,ru"
		XKBVARIANT=""
		XKBOPTIONS="grp:alt_shift_toggle"
		BACKSPACE="guess"

		
#autostart
	win > preferences > BunsenLabs > edit autostart 
	home/<user>/.config/bunsen/autostart

## Как отключить тачпад на bunsenlab?
	egrep -i 'synap|alps|etps|elan' /proc/bus/input/devices
	xinput list
	# virtual core pointer synaptics s3203
		xinput set-prop 12 'Device Enabled' 0

	#autostart
	/home/r0-bl/.config/bunsen/autostart
		xinput set-prop 12 'Device Enabled' 0


## Как изменить шорты?
	# /home/r0-bl/.xbindkeysrc
	# win > preferences > keybinds > Edit .xbindkeysrc
	# win > preferences > keybinds > restart

		# телеграмм
		"/home/r0-bl/Downloads/Telegram/Telegram"
			Mod4 + Alt + t

		# Show dmenu
		"~/.config/dmenu/dmenu-bind.sh"
			Mod4 + Shift + d

		#flameshot
		"/usr/bin/flameshot gui -d 3000"
			Mod4 + Print

## сломалось?
    bl-user-setup --reset
    // --help

# ssh + git
	#перевод ветки репа на ssh адресс вместо https который требует токен
		git remote remove origin
		git remote add origin git@github.com:R0-32/conspect.git
		git remote -v
	#создание ssh ключа и привязывание его к гитхабу
		ssh-keygen -t rsa -b 4096 -C "wodrom@gmail.com"
	# ~/.ssh/id_rsa (приватный ключ) и ~/.ssh/id_rsa.pub (публичный ключ).
	# скопировать cat ~/.ssh/id_rsa.pub
	# вставить https://github.com/settings/ssh/new
		git config --global user.name "R0-32"
		git config --global user.email "wodrom@gmail.com"
		git config --global core.sshCommand "ssh -i ~/.ssh/id_rsa"





# алиас + гит
	~/.gitconfig или git config --global --edit
	[alias]
		acp = !git add . && git commit -m "auto-commit" && git push origin main
		// del = curl -X DELETE -H "Authorization: token ssh-rsa  wodrom@gmail.com" https://api.github.com/repos/R0-32/

#узри процесс Conky и путь к файлу
	ps aux | grep conky
	geany /home/r0-bl/.config/conky/BL-Beryllium-conky.conf
	#/home/r0-bl/.xbindkeysrc
	#создал копию с другим названием
	#и через вин>preferences>Conky>Conky Manager добавил копию
		в коде изменил alignment = 'top_left',
			получил две колонки

#бекапы
	# План 0 
		bl-user-setup --reset

	# План А
		sudo apt install timeshift
			создать снимок в хомяка

	# План Б
		// упаковать
		sudo tar czvf settings.tar.gz ~/.config
		// распаковать
		tar xzvf settings.tar.gz
		// заменить
		mv ~/.config ~/

	# План В
		сохранить файлы с точкой из хомяка
			скинул в gdrive
		Установить bunsenlabs
		Вбить в терминал
			sudo apt update && sudo apt upgrade
			sudo apt install emacs timeshift synaptic ibus fancontrol ffmpeg flameshot neofetch msttcorefonts audacity git ibus
			sudo dpkg-reconfigure keyboard-configuration
			sudo service keyboard-setup restart
			#переключение в ibus
				gsettings set org.freedesktop.ibus.general.hotkey triggers "['<Alt>Shift_L']"
			telegram download from website
			vs code
				install binary
				2clic


















		вернуть в хомяка (операция провалилась ПОЛНОСТЬЮ)
			~/.config/openbox/autostart/
			~/.config/openbox/bunsen/autostart
			~/.config/conky/
			~/.config/blob/
			~/.gitconfig
			~/.ssh/
			~/.xbindkeysrc


	sudo apt install genisoimage
#запаковываю в архив - tar -cf имя_архива.tar исходный_каталог 
	sudo tar -czvf /home/r0-bl/r0_backup.tar.gz --exclude=/home/r0-bl/r0_backup.tar.gz --one-file-system -C /*
	- смотрю адрес флешки
	lsblk
	- перемещаю архив на флешку
	sudo mv /home/r0-bl/r0_backup.tar.gz /media/r0-bl/BUNSENLABS_BERYLLIUM
	- с флешки распаковываем архив в корень ноута
	sudo tar -xzvf /media/r0-bl/BUNSENLABS_BERYLLIUM/r0_backup.tar.gz -C /

sudo dd if=/dev/sda1 of=/path/to/backup.img

#бекап rsync
sudo rsync -aAXv --exclude=/home/r0-bl/r0_backup/ /* /home/r0-bl/r0_backup/

0) Файлы которые я хочу сберечь из /home/r0-bl/Downloads/ я перебрасываю в /

дальше 
1) создать образ
sudo dd if=/dev/sda5 of=/home/r0-bl/r0_backup.img bs=4M

2) создать директорию
mkdir /home/r0-bl/r0_backup/

3) примонтировать к ней созданный образ
sudo mount /home/r0-bl/r0_backup.img /home/r0-bl/r0_backup/

4) Создать образ ISO из /home/r0-bl/r0_backup
genisoimage -o /home/r0-bl/r0_backup.iso -R -J /home/r0-bl/r0_backup

5) создать загрузочную флешку
sudo dd if=/home/r0-bl/r0_backup.iso of=/dev/sdb1 bs=4M status=progress && sync

6.1) дальше загружаясь с флешки я попаду в меню где мне предложат нажать восстановление системы и сделать хорошо

6.2)там же я смогу найти терминал и восстановится введя
sudo dd if=/home/r0-bl/r0_backup.img of=/dev/sda5 bs=4M


duplicity --encrypt-key ABC123 / /home/r0-bl/duplicity-backup
duplicity --encrypt-key ABC123 /home/r0-bl/duplicity-backup /
